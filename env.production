# === Environment ===
DDBJ_SEARCH_ENV=production

# === Nginx Port Binding ===
DDBJ_SEARCH_NGINX_BIND_HOST=0.0.0.0
DDBJ_SEARCH_NGINX_BIND_PORT=18080

# === Elasticsearch Proxy (optional) ===
# コメントを外すと /search/resources で ES を読み取り専用で露出する
# DDBJ_SEARCH_ES_ENABLED=true
