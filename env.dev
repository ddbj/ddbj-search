# === Environment ===
DDBJ_SEARCH_ENV=dev

# === Nginx Port Binding ===
DDBJ_SEARCH_NGINX_BIND_HOST=127.0.0.1
DDBJ_SEARCH_NGINX_BIND_PORT=8080

# === Elasticsearch Proxy (optional) ===
# コメントを外すと /search/resources で ES を読み取り専用で露出する
# DDBJ_SEARCH_ES_ENABLED=true
